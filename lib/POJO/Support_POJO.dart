class Support_POJO
{
  String url;
  String text;

  Support_POJO(this.url, this.text);

  // factory Support_POJO.fromJson(dynamic json) {
  //   return Support_POJO(
  //       url: json['url'],
  //       text: json['text']);
  // }
}